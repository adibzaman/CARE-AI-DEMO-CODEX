# SpeziIntegration

This folder is a minimal adapter point for Stanford Spezi integration.

- `SpeziAppAdapter` can own composition root migration from `AppState` to Spezi modules.
- Replace `MockHealthDataProvider` with Spezi HealthKit support.
- Replace `RealFHIRRepositoryPlaceholder` with Spezi FHIR stack once credentials are available.
